---
phase: 02-opentelemetry-specialist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/opentelemetry-dotnet/SKILL.md
  - skills/opentelemetry-dotnet/references/signals-and-conventions.md
  - skills/opentelemetry-dotnet/references/exporters-and-collector.md
  - skills/opentelemetry-dotnet/references/troubleshooting.md
  - skills/opentelemetry-dotnet/examples/happy-path.md
  - skills/opentelemetry-dotnet/examples/boundary-handoff.md
autonomous: true
requirements:
  - OBS-01
  - OBS-02
user_setup: []
must_haves:
  truths:
    - "Agents can use `opentelemetry-dotnet` for traces, metrics, logs, conventions, correlation, and OTLP-first guidance in distributed .NET services."
    - "The skill clearly distinguishes its ownership from `aspire`, `masstransit`, and the orchestrator."
    - "The examples make the repo's distributed-.NET baseline visible rather than reading like generic observability snippets."
  artifacts:
    - path: skills/opentelemetry-dotnet/SKILL.md
      provides: Canonical observability specialist guidance
      contains: "Boundary And Handoff Rules"
    - path: skills/opentelemetry-dotnet/examples/happy-path.md
      provides: Aspire-based observability example
      contains: "OTLP"
    - path: skills/opentelemetry-dotnet/examples/boundary-handoff.md
      provides: Boundary example crossing into Aspire and MassTransit
      contains: "orchestrator"
  key_links:
    - from: skills/opentelemetry-dotnet/SKILL.md
      to: skills/opentelemetry-dotnet/references/signals-and-conventions.md
      via: "Core skill points to focused conventions guidance"
    - from: skills/opentelemetry-dotnet/examples/boundary-handoff.md
      to: skills/aspire/SKILL.md
      via: "Boundary example names Aspire as a peer owner"
    - from: skills/opentelemetry-dotnet/examples/boundary-handoff.md
      to: skills/masstransit/SKILL.md
      via: "Boundary example names MassTransit as a peer owner"
---

<objective>
Author the core OpenTelemetry specialist package so the repo gains a real observability peer for `aspire` and `masstransit`.

Purpose: Deliver the canonical `opentelemetry-dotnet` skill, focused references, and the required example pair before adding validation and adapter polish.
Output: A full core skill package with `SKILL.md`, references, and realistic examples.
</objective>

<execution_context>
@C:/Users/james/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-opentelemetry-specialist/2-CONTEXT.md
@.planning/phases/02-opentelemetry-specialist/02-RESEARCH.md
@.planning/phases/02-opentelemetry-specialist/02-VALIDATION.md
@skills/_standard/package-standard.md
@skills/_standard/skill-sections.md
@skills/_standard/boundary-and-portability.md
@skills/_standard/reference-and-validation-policy.md
@skills/aspire/SKILL.md
@skills/masstransit/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical OpenTelemetry skill body</name>
  <files>skills/opentelemetry-dotnet/SKILL.md</files>
  <action>Author `SKILL.md` using the fixed Phase 1 section contract. Make traces, metrics, and logs first-class, emphasize service identity and semantic conventions, keep the default path OTLP-first, and define the precise boundary with `aspire` and `masstransit`. The handoff section must preserve the shared repo rule: direct handoff only when one next owner is clear; otherwise return to the orchestrator.</action>
  <verify>Test-Path 'skills/opentelemetry-dotnet/SKILL.md'; Select-String -Path 'skills/opentelemetry-dotnet/SKILL.md' -Pattern 'traces|metrics|logs|service.name|semantic conventions|OTLP|aspire|masstransit|orchestrator'</verify>
  <done>`SKILL.md` exists and expresses equal-signal observability ownership plus explicit peer boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused references for conventions, exporters, and troubleshooting</name>
  <files>skills/opentelemetry-dotnet/references/signals-and-conventions.md, skills/opentelemetry-dotnet/references/exporters-and-collector.md, skills/opentelemetry-dotnet/references/troubleshooting.md</files>
  <action>Create three focused reference files instead of one large dump. One should cover traces/metrics/logs plus resource identity and conventions, one should cover OTLP/exporters/collector guidance, and one should cover the common failures this skill owns. Each file must cite official sources, and the package must record a current version or baseline where it matters for .NET/OpenTelemetry guidance so the skill satisfies the shared reference policy rather than only naming topics.</action>
  <verify>Get-ChildItem 'skills/opentelemetry-dotnet/references' -Recurse | Select-Object FullName; Select-String -Path 'skills/opentelemetry-dotnet/references/*.md' -Pattern 'OTLP|collector|semantic conventions|correlation|missing|https://|baseline|version'</verify>
  <done>The skill has focused references aligned to the discussed coverage and neutrality decisions, with official-source coverage and explicit version/baseline notes where relevant.</done>
</task>

<task type="auto">
  <name>Task 3: Create the required happy-path and boundary examples</name>
  <files>skills/opentelemetry-dotnet/examples/happy-path.md, skills/opentelemetry-dotnet/examples/boundary-handoff.md</files>
  <action>Create a realistic but focused Aspire-based happy-path example and a boundary example that crosses into both `aspire` and `masstransit`. The boundary example must show that OpenTelemetry owns telemetry policy and correlation, while hosting/runtime composition and messaging behavior are handed off or returned to the orchestrator as required.</action>
  <verify>Test-Path 'skills/opentelemetry-dotnet/examples/happy-path.md'; Test-Path 'skills/opentelemetry-dotnet/examples/boundary-handoff.md'; Select-String -Path 'skills/opentelemetry-dotnet/examples/happy-path.md' -Pattern 'Aspire|OTLP'; Select-String -Path 'skills/opentelemetry-dotnet/examples/boundary-handoff.md' -Pattern 'aspire|masstransit|orchestrator|next owner'</verify>
  <done>The example pair exists and demonstrates both in-scope observability guidance and multi-owner boundary behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `Test-Path 'skills/opentelemetry-dotnet/SKILL.md'`
- [ ] `Get-ChildItem 'skills/opentelemetry-dotnet/references','skills/opentelemetry-dotnet/examples' -Recurse`
- [ ] `Select-String -Path 'skills/opentelemetry-dotnet/SKILL.md','skills/opentelemetry-dotnet/references/*.md' -Pattern 'traces|metrics|logs|OTLP|service.name|semantic conventions|https://|baseline|version'`
- [ ] Manual review confirms the skill treats all three signals as first-class and names the correct boundaries with `aspire` and `masstransit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo now has a real OpenTelemetry specialist package that satisfies the core of `OBS-01` and establishes the `OBS-02` boundary model
  </success_criteria>

<output>
After completion, create `.planning/phases/02-opentelemetry-specialist/02-01-SUMMARY.md`
</output>
